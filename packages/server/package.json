{
  "name": "@agentopolis/agnes-server",
  "version": "1.0.1",
  "description": "Agnes A2A Server Runtime",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsup src/server.ts --format esm --dts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["a2a", "agent", "server", "runtime", "framework"],
  "author": "Ryan Semerau <ryan@agentopolis.com>",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@agentopolis/agnes-types": "~1.0.0",
    "fastify": "^4.23.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.13",
    "nodemon": "^3.1.10",
    "tsup": "^8.4.0",
    "tsx": "^4.19.4",
    "typescript": "^5.0.0",
    "vitest": "^3.1.2"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
